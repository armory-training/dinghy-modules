{
    "name": "Let's wait for the world to end",
    "refId": "{{ var "refId" ?: "wait-for-the-world-end" }}",
    "requisiteStageRefIds": {{ var "requisiteStageRefIds" ?: [] }},
    "type": "wait",
    "waitTime": {{ var "waitTime" ?: 199000 }}
}
