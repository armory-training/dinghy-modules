{
    "name": "Wait for the world to end",
    "refId": {{ var "refId" ?: 0 }},
    "requisiteStageRefIds": {{ var "requisiteStageRefIds" ?: [] }},
    "type": "wait",
    "waitTime": {{ var "waitTime" ?: 100000 }}
}
